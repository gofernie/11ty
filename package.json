{
  "name": "fernie-microsite-11ty",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "clean": "rimraf _site .cache"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "csv-parse": "^5.5.6"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
