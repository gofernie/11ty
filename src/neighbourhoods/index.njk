---
layout: base
title: Fernie Neighbourhoods
permalink: /neighbourhoods/
hero:
  src: "/img/hero/fernie-hero.webp"
  fallback: "/img/hero/bob.jpg"
  alt: "Fernie"
  title: "Fernie Neighbourhoods"
  subtitle: "Browse every area at a glance."
---

<section aria-labelledby="grid-title">
  <h2 id="grid-title" style="margin-top:0">All neighbourhoods</h2>

  <div class="grid-tiles">
    {% for n in neighbourhoods %}
      {% set slug = n.slug or (n.name | lower | replace(' ', '-') | replace('&','and')) %}
      <article class="card">
        <a href="/neighbourhoods/{{ slug }}/" style="display:block">
          <div class="card__media">
            <picture>
              <source srcset="/img/neighbourhoods/{{ slug }}.webp" type="image/webp">
              <img src="/img/neighbourhoods/{{ slug }}.jpg" alt="{{ n.name }}">
            </picture>
            <span class="badge">{{ n.name }}</span>
          </div>
          <div class="card__body">
            <h3 class="card__title">{{ n.name }}</h3>
            <p class="card__text">{{ n.summary }}</p>
          </div>
        </a>
      </article>
    {% endfor %}
  </div>
</section>
