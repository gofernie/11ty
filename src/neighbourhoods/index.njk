---
layout: base
title: "Neighbourhoods"
permalink: /neighbourhoods/
---

{# src/neighbourhoods/index.njk #}

<section class="hoods">
  <h1>Neighbourhood Guides</h1>
  <p class="intro">
    Explore Fernie’s neighbourhoods and discover the vibe, amenities, and housing styles in each area.
  </p>

  <div class="cards">
    {# `neighbourhoods` comes from src/_data/neighbourhoods.js #}
    {% set list = neighbourhoods | default([]) %}

    {% for n in list %}
      {% if n.slug %}
        <a class="card" href="/neighbourhoods/{{ n.slug | slug }}/">
          <h3>{{ n.title }}</h3>
          {% if n.desc %}<p>{{ n.desc }}</p>{% endif %}
        </a>
      {% endif %}
    {% endfor %}
  </div>
</section>
