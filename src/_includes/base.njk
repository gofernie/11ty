<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{ title or "Fernie Neighbourhood Guides" }}</title>
  <meta name="description" content="{{ description or 'Fast, mobile-first guides for Fernie real estate.' }}" />

  <!-- Minimal, LCP-friendly styles -->
  <style>
    :root{
      --ink:#0f172a; --muted:#475569; --bg:#fff; --card:#f8fafc; --line:#e2e8f0; --brand:#0ea5e9;
      --radius:16px;
      --container: 980px;
    }
    *{box-sizing:border-box}
    html,body{margin:0}
    body{
      font-family: system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,"Helvetica Neue",Arial,sans-serif;
      color:var(--ink); background:var(--bg); line-height:1.6;
    }
    .wrap{max-width:var(--container); margin:0 auto; padding:16px}
    header{border-bottom:1px solid var(--line); background:#fff; position:sticky; top:0; z-index:10}
    nav a{color:inherit; text-decoration:none; margin-right:14px}
    nav a:hover{text-decoration:underline}
    h1{font-size:clamp(1.8rem,5vw,2.4rem); line-height:1.2; margin:.6rem 0 .8rem}
    h2{font-size:1.4rem; margin:1.2rem 0 .4rem}
    h3{font-size:1.2rem; margin:1.1rem 0 .35rem}
    .lead{font-size:1.05rem; color:var(--muted)}
    .grid{display:grid; gap:14px}
    @media (min-width:720px){ .grid.cols-3{grid-template-columns: repeat(3, 1fr);} .grid.cols-2{grid-template-columns: repeat(2, 1fr);} }
    .card{background:var(--card); border:1px solid var(--line); border-radius:var(--radius); overflow:hidden}
    .card a{display:block; color:inherit; text-decoration:none}
    .card .media{aspect-ratio: 16/9; background:#e2e8f0}
    .card .pad{padding:12px 14px}
    .pill{display:inline-block; font-size:.8rem; padding:.2rem .55rem; border:1px solid var(--line); border-radius:999px; margin:0 8px 8px 0; background:#fff}
    .btn{display:inline-block; padding:.6rem .9rem; border:1px solid #334155; border-radius:.6rem; text-decoration:none}
    .btn.brand{border-color:var(--brand)}
    .heroimg picture, .heroimg img{display:block; width:100%; height:auto}
    .section{padding:12px 0}
    .muted{color:var(--muted)}
    .breadcrumbs{font-size:.9rem; color:var(--muted); margin:.4rem 0 1rem}
    footer{border-top:1px solid var(--line); margin-top:28px}
  </style>
</head>
<body>
  <header>
    <div class="wrap">
      <nav aria-label="Main">
        <a href="/">Home</a>
        <a href="/neighbourhoods/">Neighbourhoods</a>
        <a href="https://fernie.realestate/recip.html">Search Listings</a>
        <a href="mailto:chris@fernie.realestate" style="float:right">Contact</a>
      </nav>
    </div>
  </header>

  <main class="wrap">
    {{ content | safe }}
  </main>

  <footer>
    <div class="wrap">
      <small class="muted">© {{ "now" | date: "%Y" }} Chris Crump PREC*</small>
    </div>
  </footer>
</body>
</html>
